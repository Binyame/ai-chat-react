# Configure nginx for larger file uploads (PDF support)
# Increases client_max_body_size to 50MB for PDF uploads

files:
  "/etc/nginx/conf.d/proxy.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      client_max_body_size 50M;
